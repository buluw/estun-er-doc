# PLCINT

存储 PLC 短整型数据，根据数据编号与 plc 端的 int 变量相对应。该变量仅支持在全局变量作用域中创建，删除，修改，复制，粘贴等操作（其中复制、粘贴操作不能跨域和跨工程）。

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| `plcNum` | int | 数据编号。 | — |
| `value` | int | 变量值。 | — |
| `saveflag` | bool | 掉电保存标记。 | — |
| `note` | string | 变量注释。 | — |
