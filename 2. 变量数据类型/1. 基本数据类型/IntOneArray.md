# IntOneArray

存储整数数组数据。该变量支持在全局，工程，程序三大变量作用域中创建，删除，修改，复制，粘贴等操作（其中复制、粘贴操作不能跨域和跨工程）。

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| `count` | int | 数据个数。 | — |
| `value` | int | 变量值。 | — |
