# SimDO

虚拟数字量输出变量。

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| `port` | int | 与该变量绑定的虚拟数字量输出端口号。 | — |
| `value` | int | 取值范围为 0 或 1，代表对应端口输出状态。 | — |
| `note` | string | 变量注释。 | — |
