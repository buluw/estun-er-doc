# COLLIPARAM

COLLIPARAM 型变量通过 index 来选择对应的碰撞自调整参数（ID）。

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| `index` | int | index 取值范围为 [0, 6]。其中 0 表示该变量无效，其它表示 ID 为 index 的碰撞自调整参数。 | — |
| `note` | string | 变量注释。 | — |
