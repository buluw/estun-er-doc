# POSITIONER

POSITIONER 型变量通过 index 来选择变位机（ID）。

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| `index` | int | index 取值范围为 [0, 6]。其中 0 表示该变量无效，其它表示变位机 ID 为 index 的坐标系。 | — |
| `note` | string | 变量注释。 | — |
