# Socket

存储 Socket 名称信息。该变量可支持用户自主创建以及修改。该变量支持在全局，工程、程序变量作用域中创建，删除，修改，复制，粘贴等操作（其中复制、粘贴操作不能跨域和跨工程）。

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| `value` | string | socket 名称。 | — |
| `note` | string | 变量注释。 | — |
