# CPOSARRAY

存储 CPOS 点位数组数据。该变量仅支持在全局变量作用域中创建，删除，修改等操作。

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| `count` | int | 数据个数。 | — |
| `CPOSARRAY[X]` | CPOS | 存储的单个 CPOS 值。 | [X] 代表数组的索引号，如 CPOSARRAY[0]、CPOSARRAY[1]。 |

