### 工程域数据文件

每个工程 project.er 目录下 prjglobal.erd 为工程数据文件。变量对应工程域，用户可编辑。工程域下的数据只可在本工程下所有程序指令中引用。

### 程序指令文件

每个工程目录下至少包含一个或多个程序指令文件，其后缀名均为.erp。该文件存放用户编辑的指令，如后续章节中介绍的 MovJ 等。

### 程序域数据文件

每个程序均有其对应名称的数据文件，其后缀名均为.erd。该文件中存放的变量只能被本程序指令引用。