# strcmp 比较字符串

strcmp 为字符串比较指令，返回值为第一个不同字符之间的 ASCII 码差值。

## 指令格式

strcmp(s1, s2)

strcmp(字符串1, 字符串2)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| s1 | string | 参与比较的字符串数据 | — |
| s2 | string | 被比较的字符串数据 | — |

**函数返回值**：返回的 ASCII 码差值，int 型变量。
