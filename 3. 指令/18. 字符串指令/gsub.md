# gsub 替换子字符串

gsub 为在字符串 s 内搜索 a 子字符串，并用字符串 b 替换 a。

## 指令格式

gsub(s, a, b)

gsub(源字符串, 搜索字符串, 替换字符串)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| s | string | 源字符串变量或常量 | — |
| a | string | 待搜索的子字符串 | — |
| b | string | 用于替换的字符串 | — |

**函数返回值**：string 型常量。
