# find2 查找子串位置

find2 为返回字符串中子字符串的位置。

## 指令格式

find2(s, sub)

find2(源字符串, 子字符串)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| s | string | 源字符串变量或常量 | — |
| sub | string | 待查找的子字符串 | — |

**函数返回值**：int 型常量，当找不到对应字符或字符串时，返回值为 -1。
