# right 截取右侧字符

right 为字符串取右指令，从字符串右侧开始截取指定数量的字符串，并将截取后的数据返回。

## 指令格式

right(str, n)

right(源字符串, 截取数量)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| str | string | 待截取的字符串变量或常量 | — |
| n | int | 待截取的数量 | — |

**函数返回值**：截取后的字符串，string 型变量。
