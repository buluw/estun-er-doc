# trimLeft 去掉左侧空格

trimLeft 为字符串左修剪指令，去掉字符串左边的空格，并将修改过后的字符串数据返回。

## 指令格式

trimLeft(str)

trimLeft(待修剪字符串)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| str | string | 待修剪的字符串变量或常量 | — |

**函数返回值**：修剪后的字符串，string 型变量。
