# byte 取ASCII码

byte 为取字符串第 n 位的字符的 ASCCII 码。

## 指令格式

byte(str, n)

byte(源字符串, 位置索引)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| str | string | 源字符串变量或常量 | — |
| n | int | 位置索引 | — |

**函数返回值**：int 型常量。
