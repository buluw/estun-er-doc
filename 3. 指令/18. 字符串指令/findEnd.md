# findEnd 反向查找位置

findEnd 为字符串反向查找指令，在字符串中找到指定字符串最后出现的位置，并返回索引号。

## 指令格式

findEnd(s, sub)

findEnd(源字符串, 待查找字符串)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| s | string | 被查找的源字符串变量或常量 | — |
| sub | string | 待查找的指定字符串变量或常量 | — |

**函数返回值**：查找后的索引号，int 型变量。
