# IToStr 整型转字符串

IToStr 为整型数据转字符串指令，将整型数据转换成字符串类型数据，并返回转换后的字符串。

## 指令格式

IToStr(val)

IToStr(待转换整型数据)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| val | int | 待转换的整型数据 | — |

**函数返回值**：转换后的字符串数据，string 型变量。
