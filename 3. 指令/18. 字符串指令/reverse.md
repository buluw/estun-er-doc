# reverse 反转字符串

reverse 为字符串反转指令，将字符串反转后返回。

## 指令格式

reverse(str)

reverse(待反转字符串)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| str | string | 待反转的字符串变量或常量 | — |

**函数返回值**：反转后的字符串，string 型变量。
