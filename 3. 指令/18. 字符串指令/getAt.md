# getAt 获取单个字符

getAt 为单个字符串获取指令，获取某一位的字符串数据，并将获取后的数据返回。

## 指令格式

getAt(str, pos)

getAt(源字符串, 位置索引)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| str | string | 待截取的字符串变量或常量 | — |
| pos | int | 待获取的位置变量或常量 | — |

**函数返回值**：获取后的字符串，string 型变量。
