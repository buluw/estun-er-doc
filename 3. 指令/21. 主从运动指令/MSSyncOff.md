# MSSyncOff 退出协同状态

该指令用于使主从机器人退出协同状态。该指令需与 MovMSSyncOn 指令组合搭配使用，且不支持嵌套使用。

## 指令格式

MSSyncOff(SyncId)
MSSyncOff(协同标识)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| SyncId | SYNCID 变量 | 机器人协同标识 ID 号，用于本机与协同机器人之间进行协同校验，主从机器人 ID 一致时才为有效协同。 | |
