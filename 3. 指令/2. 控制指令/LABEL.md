# LABEL 标签

该指令用于定义 GOTO 跳转目标标签。

## 指令格式
LabelName:
标签名称:
*(备注：带 [] 参数为可选参数)*
---
## 参数详细说明
| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| LabelName | label | 定义的标签名称，用于 GOTO 指令跳转目标 | 无 |
---
## 应用示例
**示例 1：**
```estun
label1:
MovJ(P0)
GOTO label1
//LABEL 指令设置标签行 label1
```
