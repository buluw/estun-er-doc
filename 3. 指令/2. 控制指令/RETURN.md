# RETURN 返回

该指令用于返回。执行该指令后，程序会跳转至程序的末尾，达到结束当前程序的作用。

## 指令格式
RETURN
RETURN
---

## 参数详细说明
| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| 无 | - | RETURN 为返回指令，无参数 | 无 |
---
## 应用示例
**示例 1：**
```estun
IF x>1 THEN
    RETURN
ENDIF
x = x + 1
/*other instructions*/
END;
```
