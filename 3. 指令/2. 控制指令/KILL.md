# KILL 停止并行程序

该指令用于停止并行运行的程序，结束由 RUN 指令启动的程序。

## 指令格式
KILL(ProgramName)
KILL(程序名)
*(备注：带 [] 参数为可选参数)*
---
## 参数详细说明
| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| ProgramName | string | 要停止的并行程序名称 | 无 |
---
## 应用示例
**示例 1：**
```estun
KILL program1
```
