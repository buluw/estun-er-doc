# CLKStart 启动时钟

启动指定的时钟变量。启动后，该时钟的 `state` 变为 `true` 并开始计时。

## 指令格式
CLKStart (clk)
CLKStart (时钟变量)

---
## 参数详细说明
| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| clk | CLOCK | 需要启动的时钟变量。 |  |
---
## 应用示例
**示例 1：**
```estun
CLKStart (CLOCK0)
//启动时钟 CLOCK0。
```
