# CLKStop 停止时钟

停止指定的时钟变量。停止后，该时钟的 `state` 变为 `false`，但计时数值不会复位。

## 指令格式
CLKStop (clk)
CLKStop (时钟变量)

---
## 参数详细说明
| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| clk | CLOCK | 需要停止的时钟变量。 |  |
---
## 应用示例
**示例 1：**
```estun
CLKStop (CLOCK0)
//停止时钟 CLOCK0。
```
