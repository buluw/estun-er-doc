# CLKRead 读取时钟计时值

读取指定时钟的当前计时值。读取后可通过变量列表查看对应的 `value`。

## 指令格式
CLKRead (clk)
CLKRead (时钟变量)

---
## 参数详细说明
| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| clk | CLOCK | 需要读取的时钟变量。 |  |
---
## 应用示例
**示例 1：**
```estun
CLKRead (CLOCK0)
//读取时钟计时值并更新存于 CLOCK0 中。
```
