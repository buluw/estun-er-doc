# ITimer 定时器中断

该指令为定时器中断指令，用于将中断检测方式设为定时检测的方式，即系统会按设置的触发时间周期性执行中断程序。

## 指令格式

ITimer(Interrupt, timeMs)

ITimer(中断标识, 触发时间)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| Interrupt | INTERRUPT 变量 | 中断唯一索引号 | 必填 |
| timeMs | int | 定时检测时间，范围为 100~3000000ms | 必填 |
