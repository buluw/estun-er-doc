# ISleep 中断检测休眠

该指令为中断检测休眠指令，用于暂停系统对中断信号的检测功能。该指令不会移除中断索引绑定的任何数据，执行 IWatch 指令后可恢复检测。

## 指令格式

ISleep(Interrupt)

ISleep(中断标识)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| Interrupt | INTERRUPT 变量 | 中断唯一索引号 | 必填 |
