# IWatch 中断检测激活

该指令为中断检测激活指令，用于恢复系统对中断信号的检测功能。

## 指令格式

IWatch(Interrupt)

IWatch(中断标识)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| Interrupt | INTERRUPT 变量 | 中断唯一索引号 | 必填 |
