# IDelete 中断解除

该指令为中断解除指令，移除系统中与该中断标识绑定的任何数据，即移除对该中断标识的检测以及对应的执行数据。

## 指令格式

IDelete(Interrupt)

IDelete(中断标识)

---

## 参数详细说明

| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| Interrupt | INTERRUPT 变量 | 中断唯一索引号 | 必填 |
