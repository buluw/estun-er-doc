# ExitCycle 退出循环

该指令为退出循环指令，执行该指令后系统会立即结束当前中断程序的运行，将 PC 指针返回到主程序的起始位置处，并从新的 PC 指针行处重新开始执行。

## 指令格式

ExitCycle()

ExitCycle()

---

## 参数详细说明

无参数。

> **备注：**
> 1. 该指令只能在中断程序中执行。
> 2. 该指令必须与 IEnter 指令搭配使用。
> 3. 执行该指令后，如果主程序中示教了名称为 "CycleLabel" 的 label，系统会跳转到该 label 处执行；否则系统将跳转到主程序首行处执行。
