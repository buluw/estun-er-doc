# Wait 延时等待

该指令用于延时等待，程序暂停执行指定的时间。

## 指令格式
Wait(Time)
Wait(时间)
*(备注：带 [] 参数为可选参数)*
---
## 参数详细说明
| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| Time | int / INT | 延时等待时间，单位：ms（毫秒） | 无 |
---
## 应用示例
**示例 1：**
```estun
Wait(1000)
//等待 1 秒
```
