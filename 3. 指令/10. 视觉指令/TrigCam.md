# TrigCam 触发相机拍摄

触发相机拍摄指令。

## 指令格式
TrigCam (ti, id)
TrigCam (相机触发参数, 相机索引参数)

---
## 参数详细说明
| 参数 | 类型 | 说明 | 默认值/备注 |
|------|------|------|------------|
| ti | string | 相机触发参数。 |  |
| id | int | 指定需要触发的相机编号。 |  |
---
## 应用示例
**示例 1：**
```estun
TrigCam ("OK", 1)
//使用字符串"OK"触发 1 号相机拍照。
```
